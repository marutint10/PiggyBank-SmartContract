const { expect } = require("chai");

describe("PiggyBank contract", function () {
  it("Deployement should assing all the funds to the owner", async function () {
    const PiggyBank = await ethers.getContractFactory("PiggyBank");
  });
  it("Should transfer all the funds to the owner", async function () {
    const PiggyBank = await ethers.getContractFactory("PiggyBank");
  });
});
